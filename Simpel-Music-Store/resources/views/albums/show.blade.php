@extends('layouts.app')

@section('content')
    <p class="text-white text-4xl">{{ $album->name }}</p>
    <p class="text-white text-4xl">{{ $album->name }}</p>
    <p class="text-white text-4xl">{{ $album->name }}</p>
    <p class="text-white text-4xl">{{ $album->name }}</p>
    <p class="text-white text-4xl">{{ $album->name }}</p>
    <p class="text-white text-4xl">{{ $album->name }}</p>
    <p class="text-white text-4xl">{{ $album->name }}</p>


@endsection
